function checkAnswer() {
    var answer = document.getElementById('answer').value
    if (answer === "9 6 2 3") {
        alert("Это правильное решение! Это и есть код от шкафчика")
    } else {
        alert("У вас ошибка!")
    }
}